<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @cocktail.name %>
</p>


<div class="card"
<%= cl_image_tag @cocktail.photo,  height: 300, width: 400, crop: :fill if  @cocktail.photo %> >
<div class="card-category"></div>
<div class="card-description">
  <h2><%=  @cocktail.name %></h2>
  <p><%= link_to 'Add Dose',
    new_cocktail_dose_path(@cocktail), class: "btn btn-small btn-success" , style: "color:white"  %></p>
  </div>

  <%= link_to 'Edit', edit_cocktail_path(@cocktail) %>
  <%= link_to 'Back', cocktails_path %>

  <%= link_to 'Add Dose',
  new_cocktail_dose_path(@cocktail) %>
